import { View, Text } from "react-native";
import React from "react";

const HPheader = () => {
	return (
		<View className="bg-slate-300 h-20 flex-row justify-center items-center">
			<Text className="text-blue-600 font-bold">Header</Text>
		</View>
	);
};

export default HPheader;
